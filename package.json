{
  "name": "annual-leave-bidding-system-tests",
  "private": true,
  "description": "Regression tests for the seniority-based leave allocation engine (allocation.js). No dependencies — uses Node's built-in test runner.",
  "scripts": {
    "test": "node --test test/allocation.test.js"
  }
}
